# hardware/workspaces.conf — Workspace rules
# Managed by nwg-displays; manual edits may be overwritten on "Apply".
# https://wiki.hyprland.org/Configuring/Workspace-Rules/

# ── Workspace → monitor assignment (examples) ────────────────
# workspace = 1, monitor:eDP-1
# workspace = 2, monitor:eDP-1
# workspace = 5, monitor:DP-2
# workspace = 6, monitor:DP-2

# ── Per-workspace decoration overrides ───────────────────────
# workspace = name:coding, rounding:false, decorate:false, gapsin:0, gapsout:0, border:false, monitor:DP-1
# workspace = 8, bordersize:8
# workspace = name:gaming, monitor:desc:Chimei Innolux Corporation 0x150C, default:true

# ── On-create rules ──────────────────────────────────────────
# workspace = 5, on-created-empty:[float] firefox
# workspace = special:scratchpad, on-created-empty:foot
