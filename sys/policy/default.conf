# sys/policy/default.conf — Active policy set
# Source order: wallust colors first (required by decoration.conf), then animations.
# To override: create user/policy/ and source from user/startup.conf or user/keybind.conf.
# To switch animations at runtime: $S/Animations.sh (sources a different $P_a/*.conf).

source = $P_w/wallust-hyprland.conf
source = $P_a/default.conf


