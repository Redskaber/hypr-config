# bootstrap/default.conf — Pipeline entry point
# Source order defines override priority: later wins.
#
# Stage 0 — Constants (paths, variables, user overrides of constants)
source = ./const.conf
source = $user/const.conf

# Stage 1 — System layer (hardware → env → core → policy)
source = $sys/default.conf
