# user/startup.conf — User exec-once additions
# Sourced AFTER sys/startup.conf — system services are already started.

# ── Input method ─────────────────────────────────────────────
exec-once = fcitx5 -d -r

# ── Examples (uncomment as needed) ───────────────────────────
# exec-once = $S/RainbowBorders.sh
# exec-once = $S/WallpaperAutoChange.sh $W
# exec-once = blueman-applet
