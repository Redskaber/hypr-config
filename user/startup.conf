# user/startup.conf — User exec-once additions
# Runs AFTER core/startup.conf so services are already up.

# ── Input method ─────────────────────────────────────────────
exec-once = fcitx5 -d -r

# ── Examples (uncomment as needed) ───────────────────────────
# exec-once = $HOME/.config/hypr/S/RainbowBorders.sh
# exec-once = $HOME/.config/hypr/S/WallpaperAutoChange.sh $HOME/Pictures/wallpapers
# exec-once = blueman-applet
