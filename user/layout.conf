# user/layout.conf — User layout engine override
# Overrides sys/layout.conf. Change layout engine or tweak gaps/borders here.

general {
    layout = scrolling      # hyprscrolling; change to dwindle or master if preferred
}

# Example: tighter gaps
# general {
#     gaps_in  = 2
#     gaps_out = 4
# }
